services:
  clinic-instagram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clinic-instagram-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./media:/app/media
