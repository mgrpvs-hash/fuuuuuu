{
  "name": "monopoly-twa",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace @monopoly/server\" \"npm run dev --workspace @monopoly/client\"",
    "build": "npm run build --workspace @monopoly/shared && npm run build --workspace @monopoly/server && npm run build --workspace @monopoly/client",
    "lint": "npm run lint --workspace @monopoly/server && npm run lint --workspace @monopoly/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
